using UnityEngine;

namespace Framework.TweenManagment
{
    /*public struct Rotate : ITweener
    {
        private GameObject mTweenObject;
        private Vector3 mFrom;
        private Vector3 mTo;
        public Rotate(GameObject tweenObject, Vector3 from, Vector3 to)
        {
            mTweenObject = tweenObject;
            this.mFrom = from;
            this.mTo = to;
        }

        void ITweener.DoAnim(float val)
        {
            mTweenObject.transform.SetRotation(CustomLerp.Action(mFrom, mTo, val));
        }

        void ITweener.DoAnimReverse(float val)
        {
            mTweenObject.transform.SetRotation(CustomLerp.Action(mTo, mFrom, val));
        }
    }

    public struct RotateLocal : ITweener
    {
        private GameObject mTweenObject;
        private Vector3 mFrom;
        private Vector3 mTo;
        public RotateLocal(GameObject tweenObject, Vector3 from, Vector3 to)
        {
            mTweenObject = tweenObject;
            mFrom = from;
            mTo = to;
        }

        void ITweener.DoAnim(float val)
        {
            mTweenObject.transform.SetLocalRotation(CustomLerp.Action(mFrom, mTo, val));
        }

        void ITweener.DoAnimReverse(float val)
        {
            mTweenObject.transform.SetLocalRotation(CustomLerp.Action(mTo, mFrom, val));
        }
    }*/
}
